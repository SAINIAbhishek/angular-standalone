import { CompositeDirective } from './composite.directive';

describe('CompositeDirective', () => {
  it('should create an instance', () => {
    const directive = new CompositeDirective();
    expect(directive).toBeTruthy();
  });
});
